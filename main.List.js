const thisFunctionLoops = () => {
  document.getElementById('.className')
  postStuffToHTML = '<h3>Cities</h3>'
  loopfuncion = () =>{
    postStuffToHTML += `<h4>${city.name}</h4>
    <p>It has a population of ${city.population}</p>`
  }
}